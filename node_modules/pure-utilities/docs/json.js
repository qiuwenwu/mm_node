const json = {
  prettify: {
    parameters: ['value', 'indent'],
    description: {
      en: 'Converts a value to JSON string. When the passed value is not a string, returns the value.',
      pl: 'Konwertuje wartość na zapis w formacie JSON. Gdy przekazana wartość nie jest łańcuchem tekstowym, zwraca niezmodyfikowaną wartość.'
    }
  }
}
module.exports = json
