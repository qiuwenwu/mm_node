/**
 * 分配
 * @param {Array} arr_1 数组1
 * @param {Array} arr_2 数组2
 * @param {string} mode 方法
 * @return {Number} 返回最小值
 */
module.exports = function distributive(arr_1, arr_2, mode) {
	if(mode === '+'){
		
	}
	else if(mode === '-'){
		
	}
	else if(mode === '/'){
		
	}
	else if(mode === '*'){
		
	}
	else if(mode === '%'){
		
	}
};